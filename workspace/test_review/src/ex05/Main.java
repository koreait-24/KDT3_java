package ex05;

public class Main {
	public static void main(String[] args) {

		// 입력 클래스 import
		// 입력할 내용 메시지 출력
		// 정수형 변수1 선언 nextInt();
		// nextLine(); //버퍼비우기
		// Student[] students = new Student[변수1];
		// 반복문(for)
		// 초기식 int i = 0;
		// 조건식 i < 변수1;
		// 증감식 i++
		// 몇번째 학생 정보입력인지 출력
		// 이름 : 출력
		// String name = nextLine();
		// 전공 입력 : Major Enum 선언
		// Major major = Major.valueOf(sc.nextLine());
		// 자바점수입력
		// int javaScore = sc.nextInt();
		// dbms 점수입력
		// int dbmsScore = sc.nextInt();
		// html 점수입력
		// int htmlScore = sc.nextInt();

		// 버퍼비우기

		// students[i] = new Student(name, majar, javaScore, dbmsScore, htmlScore);
		// }
		// 성적출력
		// for(Student s : students){
//			syso(s.getName() + 님의 전공은 + s.getMajor().getKoreanName() + ""입니다);

		// }
	}
}
